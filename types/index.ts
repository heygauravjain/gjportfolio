/**
 * Types Barrel Export
 *
 * Central export point for all TypeScript interfaces and types.
 * This allows for clean imports throughout the application.
 *
 * Usage:
 * import { ResumeData, UIText, ProjectLink } from "@/types";
 */

// Resume-related types
export * from "./resume";

// Site configuration types
export * from "./site-config";
